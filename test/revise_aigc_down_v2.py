from pathlib import Path

src = Path(r"g:/My_code/Agent_atuo_graduation_project_of_GDUT/test/verilog-i2c-thesis_AIGC_down.md")
dst = Path(r"g:/My_code/Agent_atuo_graduation_project_of_GDUT/test/verilog-i2c-thesis_AIGC_down_v2.md")
text = src.read_text(encoding="utf-8")

replacements = [
    (
        "I2C 最早由 Philips 公司提出，后来由 NXP 继续维护并形成较为完整的协议文档体系。NXP 的 UM10204 手册对总线电气连接、数据帧格式、时钟同步、仲裁、ACK/NACK 应答以及多种速率模式作出了系统说明。进行硬件控制器设计时，起始与停止条件的判定、SDA 采样时刻、SCL 拉伸以及多器件共享总线等问题，都需要以该规范为主要依据。",
        "I2C 协议起源于 Philips 公司提出的板级串行通信方案，后续由 NXP 持续维护并发布规范文档。UM10204 手册对两线连接方式、传输格式、应答机制、仲裁过程、时钟同步以及不同速率等级进行了说明。本文在分析控制器实现时，主要参考其中关于起始/停止条件、SDA 采样窗口、SCL 拉伸和多器件共享总线的规定，以保证 RTL 行为与协议要求相一致。",
    ),
    (
        "从已有工程实现看，I2C 控制器往往作为 FPGA 平台 IP、SoC 片上外设或开源 RTL 模块被复用。AMD/Xilinx 提供的 AXI IIC Bus Interface IP 采用 AXI 总线接入方式，使处理器能够通过寄存器发起 I2C 传输；OpenCores 的 I2C controller core 则面向 Wishbone 总线生态，展示了开源 SoC 中常见的寄存器控制与总线封装思路。这些资料说明，I2C 控制器的工程价值不仅在于协议本身，还体现在其与片上总线和验证环境的结合方式上。",
        "在实际工程中，I2C 控制器通常不会孤立存在，而是以可复用 IP 的形式接入 FPGA 或 SoC 系统。AMD/Xilinx 的 AXI IIC Bus Interface IP 采用 AXI 外设接口，便于处理器通过寄存器完成配置与传输控制；OpenCores 的 I2C controller core 则面向 Wishbone 总线，体现了开源 SoC 生态中的外设封装方式。由此可见，控制器设计除了满足 I2C 时序，还需要考虑上层总线、寄存器模型和验证平台的配合。",
    ),
    (
        "I2C 属于同步串行通信总线，基本信号只有串行时钟 SCL 和串行数据 SDA。空闲状态下，两根线在上拉作用下保持高电平；当 SCL 为高电平且 SDA 由高变低时，主设备发出起始条件；当 SCL 为高电平且 SDA 由低变高时，则表示停止条件。起始条件用于通知总线上的从设备一次传输即将开始，停止条件则表示本次传输结束并释放总线。",
        "I2C 是一种以时钟同步为基础的串行总线，通信时主要使用 SCL 与 SDA 两根线。总线空闲时，外部上拉电阻使两根线保持高电平。主设备若要开始一次访问，需要在 SCL 保持高电平时将 SDA 从高电平拉低；若要结束访问，则在 SCL 为高电平时释放 SDA，使其由低电平回到高电平。前者对应起始条件，后者对应停止条件。",
    ),
    (
        "Verilog HDL 常用于描述数字电路中的组合逻辑、时序逻辑、有限状态机和模块层次结构。与软件语言不同，Verilog 代码最终需要映射为寄存器、查找表、存储器和互连资源。FPGA 提供了可重新配置的逻辑单元、触发器、片上 RAM、时钟管理模块和 I/O 资源，使设计者能够在开发板或目标硬件上实现并验证数字系统。",
        "Verilog HDL 面向硬件结构描述，可用于表达组合逻辑、时序寄存器、有限状态机以及模块之间的连接关系。其代码在综合后并不是按软件语句顺序执行，而是被映射到 FPGA 内部的查找表、触发器、存储资源和布线网络中。借助 FPGA 提供的可编程逻辑、片上 RAM、时钟管理和 I/O 资源，设计者可以在硬件平台上快速实现和调试数字系统。",
    ),
    (
        "典型 FPGA 开发流程包括 RTL 编写、功能仿真、综合、布局布线、时序检查以及板级验证等环节。通信控制器往往需要同时处理位时序、字节传输和上层接口握手，若状态划分不清，后续调试会比较困难。因此，I2C 控制器适合采用分层状态机：底层负责 SCL/SDA 的位级动作，上层负责命令解释、读写方向和事务结束条件。",
        "FPGA 工程通常要经历 RTL 设计、功能仿真、综合实现、布局布线、时序约束检查以及实物验证等步骤。对于通信接口而言，控制逻辑既要关注每一位在总线上的变化，也要管理字节级数据和上层握手信号。若将这些逻辑混在一起，状态转换会变得复杂。因此，本课题中的 I2C 控制器更适合把位级时序和事务级控制分开组织。",
    ),
    (
        "AXI 是 AMBA 总线体系中的常用接口规范，其中 AXI Stream 更侧重连续数据流传输。该接口的基本规则是：发送端用 `tvalid` 表示当前数据有效，接收端用 `tready` 表示可以接收；只有两者同时为高时，当前拍的数据才完成传递。`tdata` 承载数据本身，`tlast` 可用于指出一帧或一次事务的末尾。",
        "AXI 属于 AMBA 规范体系中的常用接口。与面向寄存器读写的总线不同，AXI Stream 更适合传递连续数据。其握手机制由 `tvalid` 和 `tready` 共同决定：发送侧声明数据有效，接收侧声明可以接收，二者同时成立时数据才在当前时钟周期完成交接。`tdata` 用于携带数据内容，`tlast` 通常用来标记帧结束或事务边界。",
    ),
    (
        "AXI Lite 则是 AXI 的简化寄存器访问接口，适合低带宽控制外设。AXI Lite 包括写地址、写数据、写响应、读地址和读数据通道，通常用于处理器访问外设寄存器。本文项目中的 `i2c_master_axil` 模块将 I2C 主机封装为 AXI Lite 从设备，提供状态、命令、数据和分频寄存器，便于软件通过寄存器发起 I2C 操作。",
        "AXI Lite 可理解为面向控制寄存器访问的轻量化 AXI 接口，常用于处理器配置低速外设。该接口由写地址、写数据、写响应、读地址和读数据等通道组成，传输带宽要求不高，但访问流程规范。项目中的 `i2c_master_axil` 便利用这一接口把 I2C 主机包装成 AXI Lite 从设备，使软件能够通过状态、命令、数据和分频等寄存器完成控制。",
    ),
    (
        "硬件设计验证不仅需要检查 RTL 语法和综合结果，还需要通过仿真验证功能正确性。Icarus Verilog 是开源 Verilog 编译和仿真工具，可将 Verilog 测试平台和 DUT 编译为可执行仿真文件。MyHDL 是基于 Python 的硬件建模和验证工具，可以用于构建高层行为模型、总线功能模型和测试激励。",
        "硬件模块完成编码后，不能只依赖语法检查判断其正确性，还需要通过仿真观察功能行为是否满足预期。Icarus Verilog 可以编译 Verilog 测试平台与被测设计，生成可运行的仿真文件；MyHDL 则利用 Python 描述测试激励、行为模型和总线交互逻辑。两者结合后，既能运行真实 RTL，又能用 Python 编写更灵活的验证环境。",
    ),
    (
        "1. 可综合性：所有 RTL 核心模块应能被 FPGA 综合工具识别和综合。\n2. 可复用性：模块接口应标准化，便于在不同 FPGA 工程中复用。\n3. 可配置性：分频、FIFO 深度、地址掩码等参数应可配置。\n4. 可验证性：应提供测试平台和行为模型，便于自动化仿真。\n5. 兼容性：应适配 AXI Stream、AXI Lite 和 Wishbone 等多种接口。\n6. 可靠性：应正确处理 ACK/NACK、总线活动状态、时钟拉伸和 FIFO 状态。",
        "1. 可综合性：核心 RTL 应采用常见综合工具能够识别的写法，避免仅适用于仿真的结构。\n2. 可复用性：模块端口和参数应保持清晰，便于迁移到其他 FPGA 工程。\n3. 可配置性：I2C 分频值、FIFO 深度和地址掩码等参数应能根据系统需求调整。\n4. 可验证性：工程应配套测试脚本和行为模型，支持自动化功能仿真。\n5. 接口兼容性：设计需要同时兼顾 AXI Stream、AXI Lite 和 Wishbone 等连接方式。\n6. 通信可靠性：控制逻辑应正确响应 ACK/NACK、总线忙闲、时钟拉伸和 FIFO 状态变化。",
    ),
    (
        "1. 代码语言为 Verilog 2001，设计风格应符合 RTL 综合习惯。\n2. I2C 总线为开漏结构，SCL/SDA 连接需通过三态或线与逻辑实现。\n3. `prescale` 决定 I2C 时钟速率，其配置应与系统输入时钟和目标 I2C 速率匹配。\n4. MyHDL 协同仿真依赖 Python、MyHDL、Icarus Verilog 和 `myhdl.vpi`。\n5. 当前材料未包含综合报告、板级验证截图和完整仿真日志，相关数据不能凭空编写。",
        "1. 工程源码采用 Verilog 2001，论文分析和后续修改都应保持可综合 RTL 风格。\n2. SCL 与 SDA 遵循开漏总线特性，顶层连接时需要正确处理三态释放和低电平驱动。\n3. `prescale` 与系统时钟、目标 I2C 速率直接相关，配置时不能脱离实际时钟条件。\n4. 若运行协同仿真，需要提前准备 Python、MyHDL、Icarus Verilog 以及 `myhdl.vpi` 环境。\n5. 目前材料没有提供完整仿真日志、综合报告和板级截图，论文中相关结果只能标注为待补充。",
    ),
    (
        "从项目结构和功能边界看，系统可以按四个层次理解：底层是直接处理 I2C 时序的协议核心层；其上是负责命令和数据缓冲的流式接口层；再上层是面向处理器或片上互连的总线封装层；外围则是由 Python 与 Verilog 测试文件组成的验证层。",
        "结合 RTL 目录和测试平台文件，可以把该设计理解为由内到外的四个部分。最内层是直接控制 SCL/SDA 的 I2C 主从协议核心；第二层使用流式接口和 FIFO 传递命令及数据；第三层通过 AXI Lite、Wishbone 等封装接入处理器或片上互连；最外层则由 Python 行为模型和 Verilog 测试顶层共同构成验证环境。",
    ),
    (
        "验证层由 `tb/` 目录下的 Python 与 Verilog 文件构成，包括 I2C 行为模型、AXI Stream 端点、AXI Lite BFM、Wishbone 模型和各模块测试脚本。验证层用于生成激励、驱动 DUT、检查返回数据并记录仿真波形。",
        "验证相关文件集中在 `tb/` 目录。Python 部分提供 I2C 行为模型、AXI Stream 端点、AXI Lite BFM 和 Wishbone 模型，Verilog 部分则给出被测模块连接和仿真顶层。测试脚本负责产生读写激励、驱动 DUT 运行、检查返回数据，并在需要时输出波形供后续分析。",
    ),
    (
        "主机侧命令接口把一次 I2C 操作拆成若干控制字段，包括目标地址、是否产生 start、读写方向、是否连续写入以及是否发送 stop 等。接口采用 valid/ready 握手，只有命令有效且主机能够接收时，该命令才会进入控制器。通过这些字段组合，上层逻辑可以描述单字节读写、多字节写以及 repeated start 等常见事务。",
        "主机命令接口并不直接传入完整波形，而是用若干字段描述一次事务的控制意图，包括目标地址、起始条件、读写方向、连续写入和停止条件等。命令传递遵循 valid/ready 规则，只有发送方声明有效且控制器处于可接收状态时才会采纳该命令。借助这些控制位，上层可组合出单字节访问、多字节写入和 repeated start 等场景。",
    ),
    (
        "数据通道以字节为单位传输，写入和读出均采用 8 位 `tdata` 配合 `tvalid`、`tready` 和 `tlast`。其中 `tvalid/tready` 决定某个字节是否真正完成交接，`tlast` 则用于标记多字节访问的边界。这种形式与 FIFO、DMA 或自定义状态机连接时较为直接，也方便测试平台构造输入输出数据流。",
        "数据接口按 8 位字节组织，写通道和读通道都使用 `tdata` 传递数据，并用 `tvalid`、`tready` 完成握手确认。多字节事务中，`tlast` 用来指出当前字节是否为本次传输的末尾。采用这种流式格式后，控制器前后可以较自然地连接 FIFO 或自定义控制状态机，测试脚本也能方便地注入和接收数据。",
    ),
    (
        "AXI Lite 封装将控制器暴露为一组寄存器。状态寄存器用于反馈 `busy`、`bus_control`、`bus_active`、`miss_ack` 以及 FIFO 空满、溢出等信息；命令寄存器保存目标地址和 start/read/write/stop 等控制位；数据寄存器承担写入数据和读回数据的交互；分频寄存器则决定 SCL 时钟生成参数。通过这种映射方式，软件无需直接处理流式握手细节。",
        "在 AXI Lite 封装中，I2C 主机被抽象成若干可读写寄存器。软件读取状态寄存器即可了解模块忙闲、总线控制权、ACK 缺失和 FIFO 状态；写命令寄存器可提交地址以及 start、read、write、stop 等控制信息；数据寄存器用于发送或接收字节；分频寄存器则设置 SCL 生成参数。这样，上层软件主要面对寄存器读写，而不用直接参与底层流式握手。",
    ),
    (
        "其中 `Fclk` 为系统输入时钟频率，`FI2Cclk` 为目标 I2C 时钟频率。通过分频计数器，模块能够在不同系统时钟下生成目标 I2C 速率。",
        "式中 `Fclk` 表示输入到控制器的系统时钟，`FI2Cclk` 表示期望得到的 I2C 总线时钟。模块内部依据该分频值推进物理层状态机，使同一 RTL 在不同系统时钟条件下仍可配置出合适的 SCL 频率。",
    ),
    (
        "`i2c_slave` 面向外部主机访问场景，需要在被动监听总线的同时完成地址识别和数据交换。由于 SCL 与 SDA 来自芯片外部，源码中使用 `FILTER_LEN` 参数对输入进行简单滤波，以减小毛刺对边沿判断的干扰。模块通过比较相邻采样周期的信号变化识别 SCL、SDA 的上升沿和下降沿，并据此判断起始条件、停止条件以及后续数据采样时机。",
        "`i2c_slave` 与主机模块不同，它不能主动决定事务何时开始，而是持续监听外部主机在总线上的操作。SCL 和 SDA 均来自芯片引脚，可能受到毛刺或边沿抖动影响，因此源码中设置 `FILTER_LEN` 对输入进行滤波。滤波后的相邻采样值用于识别 SCL、SDA 的边沿变化，进而判断 start、stop 以及每一位数据的采样窗口。",
    ),
    (
        "`i2c_init` 主要服务于系统上电后的自动配置需求。该模块把初始化流程预先写入 `init_data` ROM，每个 9 位表项对应一种控制含义，例如选择目标地址、写入数据、插入延时或发送 stop 等。这样在没有处理器参与的情况下，FPGA 逻辑也可以按照固定顺序向 I2C 外设写入配置序列。",
        "`i2c_init` 用于处理上电后固定外设配置这类场景。设计者可以把配置流程写入 `init_data` ROM，模块运行时逐项读取表项并解释为地址选择、数据写入、延时等待或 stop 等动作。对于不包含 CPU 的纯 FPGA 系统，这种方式可以让硬件在启动阶段自动完成部分 I2C 外设初始化。",
    ),
    (
        "`axis_fifo` 并不只服务于 I2C 模块，而是一个较通用的 AXI Stream 缓冲单元。它可以通过参数调整深度、数据宽度以及 `tkeep`、`tlast`、`tid`、`tdest`、`tuser` 等附加信号是否启用，也可配置输出流水级和帧级 FIFO 行为。I2C 封装模块利用该 FIFO 缓冲命令、写数据和读数据，从而减轻上层访问节奏与总线传输节奏之间的不匹配。",
        "`axis_fifo` 是项目中复用性较强的流式缓冲模块。除了基本的数据宽度和深度参数外，它还可以按需启用 `tkeep`、`tlast`、`tid`、`tdest`、`tuser` 等 AXI Stream 附加信号，并支持不同输出流水和帧处理选项。放在 I2C 控制器封装中时，该 FIFO 主要用于暂存命令、待写数据和已读数据，缓解寄存器访问速度与 I2C 总线速度之间的差异。",
    ),
    (
        "项目 README 说明，运行测试平台需要 MyHDL、Icarus Verilog，并正确安装 `myhdl.vpi`。测试文件位于 `tb/` 目录，包括 Python 测试脚本和 Verilog 测试顶层。典型测试流程为：Python 脚本调用 Icarus Verilog 编译 RTL 与 Verilog testbench，生成 `.vvp` 文件；随后通过 `vvp -m myhdl` 加载 MyHDL VPI，使 Python 行为模型与 Verilog DUT 进行协同仿真。",
        "根据项目 README，测试环境需要安装 MyHDL、Icarus Verilog，并保证 `myhdl.vpi` 能被仿真器加载。测试资料主要放在 `tb/` 目录，其中既有 Python 测试脚本，也有 Verilog 测试顶层。一般流程是先由脚本调用 Icarus Verilog 编译 RTL 和 testbench，生成 `.vvp` 仿真文件，再通过 `vvp -m myhdl` 启动协同仿真，使 Python 行为模型与 Verilog DUT 同步运行。",
    ),
    (
        "主机写测试关注控制器能否把上层写命令转换成完整的 I2C 波形。测试时先向命令接口给出目标地址以及 start、write、stop 等控制位，再向写数据通道送入一个或多个字节。随后检查 SCL/SDA 上是否依次出现起始条件、地址字段、写方向位、数据位、ACK 周期和停止条件，同时观察 `busy`、`bus_active` 等状态是否与事务过程一致。",
        "主机写测试的重点是确认控制器能否按照命令完成一次规范写传输。测试平台先向命令通道提交从设备地址以及 start、write、stop 等控制信息，再通过写数据通道送入待发送字节。验证时需要检查 SCL/SDA 波形中是否包含起始条件、地址与写方向位、数据位、ACK 周期和停止条件，并同步观察 `busy`、`bus_active` 等状态变化。",
    ),
    (
        "主机读测试用于检查控制器从外部从设备取数的能力。测试平台先在 I2C 存储器模型中准备待读内容，再通过命令接口发起读事务。验证重点包括：主机是否正确发送地址和读方向位，读回字节是否出现在 `m_axis_data_tdata`，以及事务末尾是否通过 `m_axis_data_tlast` 正确标识。",
        "主机读测试用于验证读事务的数据返回路径。测试前，I2C 存储器模型中需要预置若干数据；测试过程中，主机通过命令接口发送读请求并驱动地址阶段。随后应检查读方向位是否正确、返回字节是否进入 `m_axis_data_tdata`，以及最后一个字节是否由 `m_axis_data_tlast` 标出。",
    ),
    (
        "多字节写测试主要检查 `write_multiple` 与 `tlast` 的配合关系，确认控制器能够连续发送多个字节并在末字节处结束本轮写入。Repeated start 测试则关注在不释放总线的条件下重新发起地址阶段的能力。许多寄存器型外设会先接收寄存器地址，再通过 repeated start 切换到读方向，因此该场景能较好反映命令层状态机处理连续事务的能力。",
        "多字节写测试关注连续数据发送过程，尤其是 `write_multiple` 命令与 `tlast` 标志之间是否配合正确。控制器应在多个字节之间保持事务连续，并在末字节后按命令决定是否停止。Repeated start 场景则模拟寄存器型外设常见访问方式：先写入寄存器地址，再不释放总线直接切换为读操作。该测试可以反映命令层状态机对组合事务的处理能力。",
    ),
    (
        "从机测试中，需要分别发送匹配地址和不匹配地址。地址匹配时，从机应输出 ACK 并进入读写状态；地址不匹配时，从机应释放总线，不应错误输出数据。通过修改 `device_address_mask`，还可测试部分地址匹配功能。",
        "从机地址匹配测试需要构造两类访问：一类使用与 `device_address` 匹配的地址，另一类使用不匹配地址。匹配时，从机应在应答周期拉低 SDA，并根据读写方向进入后续数据阶段；不匹配时，从机应保持释放状态，不应参与数据传输。若改变 `device_address_mask`，还可以进一步观察掩码匹配对地址响应范围的影响。",
    ),
    (
        "当外部主机读取从机，而从机本地输入数据尚未准备好时，从机应拉低 SCL 进行时钟拉伸。测试中可人为暂停 AXI Stream 输入侧数据，观察 SCL 是否被保持为低电平，并在数据有效后恢复传输。",
        "时钟拉伸测试主要面向从机读方向。测试平台可以故意延迟 AXI Stream 输入侧的数据，使从机在外部主机请求读取时暂时没有可发送字节。此时应观察 SCL 是否被从机保持在低电平；当数据重新有效后，SCL 应被释放，后续位传输继续进行。",
    ),
    (
        "2. 测试平台并非只包含简单激励文件，还提供了 I2C 行为模型、AXI Stream 端点、AXI Lite BFM 与 Wishbone 模型。这些组件可以分别模拟外部 I2C 设备、流式数据源/接收端以及片上总线访问过程，因此能够围绕主机、从机和总线封装模块构造较完整的验证场景。",
        "2. 测试平台包含多类辅助模型，不仅能够产生简单激励，还可以模拟外部 I2C 设备、AXI Stream 数据源与接收端，以及 AXI Lite、Wishbone 总线访问过程。依托这些模型，可以围绕主机核心、从机核心和总线封装模块分别搭建验证场景。",
    ),
    (
        "本文以 Verilog I2C interface 项目为基础，对基于 Verilog 的 I2C 总线控制器进行了设计整理与验证分析。论文从 FPGA 板级低速外设访问的实际需求出发，说明了硬件 I2C 控制器在外设初始化、传感器访问和 SoC 外设扩展中的作用；随后梳理 I2C 协议、Verilog HDL、FPGA、AXI Stream、AXI Lite、Wishbone 以及 MyHDL/Icarus Verilog 协同仿真等内容，为后续章节的系统分析提供技术背景。",
        "本文围绕 Verilog I2C interface 项目，对 I2C 总线控制器的结构、接口和验证方法进行了整理分析。论文首先从 FPGA 板级外设管理需求入手，说明 I2C 控制器在 EEPROM、传感器、时钟芯片和 SoC 外设扩展中的应用价值；随后介绍 I2C 协议、Verilog HDL、FPGA、AXI Stream、AXI Lite、Wishbone 以及 MyHDL/Icarus Verilog 协同仿真，为后续需求分析和模块设计提供基础。",
    ),
    (
        "系统实现章节围绕若干关键 RTL 模块展开。`i2c_master` 通过命令层和物理层两个状态机分别处理事务控制与位级时序；`i2c_slave` 依靠地址匹配、输入滤波、流式数据转换和时钟拉伸完成从设备响应；`i2c_master_axil` 与 `i2c_master_wbs_8` 将主机核心包装成寄存器外设；`i2c_init` 使用 ROM 表描述上电配置序列；`axis_fifo` 则为命令和数据通道提供缓冲能力。",
        "在实现分析中，论文分别讨论了项目中的几个关键模块。`i2c_master` 负责将上层命令转换为具体 I2C 时序，`i2c_slave` 负责监听外部主机并完成地址响应和数据交换，`i2c_master_axil`、`i2c_master_wbs_8` 分别提供 AXI Lite 与 Wishbone 寄存器接口，`i2c_init` 用 ROM 表组织上电初始化流程，`axis_fifo` 则承担命令与数据缓冲作用。",
    ),
    (
        "同时，感谢开源社区公开 Verilog I2C interface 项目及相关说明资料。该项目中的主从控制器、AXI Lite/Wishbone 封装和 MyHDL 协同仿真测试平台，为本文完成系统分析提供了重要参考。还要感谢同学和朋友在资料查找、环境准备和论文修改过程中给予的交流与帮助。",
        "还要感谢开源社区对 Verilog I2C interface 项目及相关资料的公开分享。项目中的主机、从机、AXI Lite/Wishbone 封装以及 MyHDL 测试平台，为本文开展模块分析和验证方案整理提供了直接参考。与此同时，同学和朋友在资料查找、环境准备和论文修改阶段给予了许多交流和帮助，在此一并表示感谢。",
    ),
]

changed = 0
missing = []
for old, new in replacements:
    if old in text:
        text = text.replace(old, new, 1)
        changed += 1
    else:
        missing.append(old[:90].replace("\n", " "))

dst.write_text(text, encoding="utf-8", newline="\n")
print(f"output={dst}")
print(f"changed={changed}")
if missing:
    print(f"missing={len(missing)}")
    for item in missing:
        print("- " + item)
